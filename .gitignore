.env

# Debug artifacts (Playwright MCP sessions)
.playwright-mcp/
tiny-sandbox-results/
tiny-sandbox-bin

# Compiled Go binary (built from apps/api/cmd/http-server)
/http-server
# Env do laboratório local (deriva do .env e carrega segredo). Nunca versionar.
.lab.env.local
.lab.prod.env.local
