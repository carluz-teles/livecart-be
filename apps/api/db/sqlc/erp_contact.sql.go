// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: erp_contact.sql

package sqlc

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const getERPContact = `-- name: GetERPContact :one

SELECT id, store_id, integration_id, platform_user_id, platform_handle, external_contact_id, created_at, updated_at FROM erp_contacts
WHERE store_id = $1 AND integration_id = $2 AND platform_user_id = $3
`

type GetERPContactParams struct {
	StoreID        pgtype.UUID `json:"store_id"`
	IntegrationID  pgtype.UUID `json:"integration_id"`
	PlatformUserID string      `json:"platform_user_id"`
}

// =============================================================================
// ERP CONTACTS (cache of Tiny contact IDs per platform user)
// =============================================================================
func (q *Queries) GetERPContact(ctx context.Context, arg GetERPContactParams) (ErpContact, error) {
	row := q.db.QueryRow(ctx, getERPContact, arg.StoreID, arg.IntegrationID, arg.PlatformUserID)
	var i ErpContact
	err := row.Scan(
		&i.ID,
		&i.StoreID,
		&i.IntegrationID,
		&i.PlatformUserID,
		&i.PlatformHandle,
		&i.ExternalContactID,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listERPContactsByStore = `-- name: ListERPContactsByStore :many
SELECT id, store_id, integration_id, platform_user_id, platform_handle, external_contact_id, created_at, updated_at FROM erp_contacts
WHERE store_id = $1
ORDER BY created_at DESC
`

func (q *Queries) ListERPContactsByStore(ctx context.Context, storeID pgtype.UUID) ([]ErpContact, error) {
	rows, err := q.db.Query(ctx, listERPContactsByStore, storeID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ErpContact{}
	for rows.Next() {
		var i ErpContact
		if err := rows.Scan(
			&i.ID,
			&i.StoreID,
			&i.IntegrationID,
			&i.PlatformUserID,
			&i.PlatformHandle,
			&i.ExternalContactID,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const upsertERPContact = `-- name: UpsertERPContact :one
INSERT INTO erp_contacts (store_id, integration_id, platform_user_id, platform_handle, external_contact_id)
VALUES ($1, $2, $3, $4, $5)
ON CONFLICT (store_id, integration_id, platform_user_id)
DO UPDATE SET
    platform_handle = EXCLUDED.platform_handle,
    external_contact_id = EXCLUDED.external_contact_id,
    updated_at = now()
RETURNING id, store_id, integration_id, platform_user_id, platform_handle, external_contact_id, created_at, updated_at
`

type UpsertERPContactParams struct {
	StoreID           pgtype.UUID `json:"store_id"`
	IntegrationID     pgtype.UUID `json:"integration_id"`
	PlatformUserID    string      `json:"platform_user_id"`
	PlatformHandle    string      `json:"platform_handle"`
	ExternalContactID string      `json:"external_contact_id"`
}

func (q *Queries) UpsertERPContact(ctx context.Context, arg UpsertERPContactParams) (ErpContact, error) {
	row := q.db.QueryRow(ctx, upsertERPContact,
		arg.StoreID,
		arg.IntegrationID,
		arg.PlatformUserID,
		arg.PlatformHandle,
		arg.ExternalContactID,
	)
	var i ErpContact
	err := row.Scan(
		&i.ID,
		&i.StoreID,
		&i.IntegrationID,
		&i.PlatformUserID,
		&i.PlatformHandle,
		&i.ExternalContactID,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
